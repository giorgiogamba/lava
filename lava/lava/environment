# Where the Vulkan SDK is located. Must end after version and OS, like "../1.3.296.0/macOS"
VULKAN_SDK_PATH = 

# Where GLSLC program is located. Must have .exe at the end if Windows
GLSLC = 

# Where GLFW is located
USER_PATHS = 